polkit.addRule(function(action, subject) {
    if (action.id == "org.freedesktop.systemd1.manage-units" || action.id == "org.freedesktop.systemd1.run-system")) {
        if (subject.user == "tie") {
            return polkit.Result.YES;
        }
    }
});
